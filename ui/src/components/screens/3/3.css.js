import styled from "styled-components";

const Wrapper = styled.div``;

const DateText = styled.div`
  font-size: 0.75rem;
  font-weight: 400;
`;

const TilesGrid = styled.div`
  display: grid;
  grid-template-columns: auto auto;
  grid-gap: 1.25rem;
  margin-top: 0.75rem;
`;

export { Wrapper, DateText, TilesGrid };
