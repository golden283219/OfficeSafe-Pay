import styled from "styled-components";

const Wrapper = styled.div`
  width: 17.813rem;
`;

const WelcomeText = styled.div`
  font-weight: 400;
  font-size: 0.75rem;
  mix-blend-mode: normal;
  margin-bottom: 0.438rem;
`;

const TitleText = styled.div`
  font-weight: bold;
  font-size: 2rem;
`;

export { Wrapper, WelcomeText, TitleText };
