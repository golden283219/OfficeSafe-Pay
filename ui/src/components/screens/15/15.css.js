import styled from "styled-components";

const Wrapper = styled.div``;

const NotificationList = styled.div`
  width: 100%;
  height: 40vh;
  overflow-y: auto;
`;

export { Wrapper, NotificationList };
